from setuptools import setup, find_packages

setup(
    name="tally_mvp",
    version="0.1.0",
    description="Tally-like MVP customizations for ERPNext — simplified UX, GST exports, India setup",
    author="Tally MVP Team",
    author_email="dev@example.com",
    packages=find_packages(),
    zip_safe=False,
    include_package_data=True,
    install_requires=[],
)
